<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'users/info', user: current_user %>
      <h2 class="mt-3">New book</h2>
      <%= render 'form', book: @new_form_book  %>
    </div>
    <div  class='col-md-8 offset-md-1 detail-group'>
      <%= render 'detail',  {book_comment: @book_comment, book: @book }   %>
    </div>
  </div>
</div>